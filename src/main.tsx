import React from 'react';
import ReactDOM from 'react-dom/client';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <h1>FIA Test Web App (Standard)</h1>
    <p>Dummy React/Vite app for FIA install flow testing.</p>
  </React.StrictMode>
);
